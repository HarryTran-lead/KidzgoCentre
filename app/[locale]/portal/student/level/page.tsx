import { LearnerGamificationWorkspace } from "@/components/gamification/learner-gamification-workspace";

export default function StudentLevelPage() {
  return <LearnerGamificationWorkspace portalLabel="Học viên" initialTab="level" />;
}
