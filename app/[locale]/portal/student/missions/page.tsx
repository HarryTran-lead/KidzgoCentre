import { LearnerGamificationWorkspace } from "@/components/gamification/learner-gamification-workspace";

export default function StudentMissionsPage() {
  return <LearnerGamificationWorkspace portalLabel="Học viên" initialTab="missions" />;
}
