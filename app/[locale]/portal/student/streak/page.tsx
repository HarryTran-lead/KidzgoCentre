import { LearnerGamificationWorkspace } from "@/components/gamification/learner-gamification-workspace";

export default function StudentStreakPage() {
  return <LearnerGamificationWorkspace portalLabel="Học viên" initialTab="streak" />;
}
