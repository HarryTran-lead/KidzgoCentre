# Visual Flow: Branch-Based Lead Filtering

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                          STAFF LOGIN                             │
│                                                                   │
│  Staff đăng nhập → Backend returns JWT with user info            │
│                    (includes branchId + branchName)              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    LEADS PAGE COMPONENT                          │
│                                                                   │
│  1. useCurrentUser() hook executes                               │
│     └─→ GET /api/auth/me                                         │
│         └─→ Returns: { branchId, branchName, ... }              │
│                                                                   │
│  2. Component waits for user data                                │
│     └─→ isLoadingUser === false                                 │
│                                                                   │
│  3. fetchInitialData() triggers                                  │
│     └─→ GET /api/leads?branchId={userBranchId}&pageSize=1000   │
│         └─→ Loads all leads for stats & filters                 │
│                                                                   │
│  4. fetchLeads() triggers (with filters)                         │
│     └─→ GET /api/leads?branchId={userBranchId}&page=1&...      │
│         └─→ Loads paginated leads                               │
└─────────────────────────────────────────────────────────────────┘
```

## Component State Flow

```
┌──────────────────┐
│  Initial State   │
│                  │
│  user: null      │
│  isLoading: true │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────┐
│  useCurrentUser() runs   │
│                          │
│  Fetching user info...   │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  User data loaded            │
│                              │
│  user: { branchId, ... }     │
│  isLoadingUser: false        │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│  useEffect triggered         │
│                              │
│  Dependencies:               │
│  - currentUser ✓             │
│  - isLoadingUser: false ✓    │
└────────┬─────────────────────┘
         │
         ├──────────────────────┐
         │                      │
         ▼                      ▼
┌────────────────────┐  ┌──────────────────┐
│ fetchInitialData() │  │   fetchLeads()   │
│                    │  │                  │
│ Stats & Filters    │  │ Paginated Data   │
│ with branchId      │  │ with branchId    │
└────────────────────┘  └──────────────────┘
```

## Data Flow with Branch Filter

```
USER DATA
┌────────────────────────────────┐
│ currentUser = {                │
│   id: "staff-123",             │
│   branchId: "branch-hn-001",   │◄── THIS IS KEY!
│   branchName: "Chi nhánh HN",  │
│   role: "Staff"                │
│ }                              │
└────────────┬───────────────────┘
             │
             ▼
API REQUEST WITH FILTER
┌────────────────────────────────────────────────┐
│ GET /api/leads?branchId=branch-hn-001&page=1   │
└────────────┬───────────────────────────────────┘
             │
             ▼
BACKEND PROCESSING
┌────────────────────────────────────────────────┐
│ SQL: SELECT * FROM leads                       │
│      WHERE branchId = 'branch-hn-001'          │
│      AND status = ... (other filters)          │
└────────────┬───────────────────────────────────┘
             │
             ▼
FILTERED RESULTS
┌────────────────────────────────────────────────┐
│ Returns only leads from "Chi nhánh HN"         │
│                                                 │
│ Lead 1: branch-hn-001 ✓                        │
│ Lead 2: branch-hn-001 ✓                        │
│ Lead 3: branch-hcm-002 ✗ (filtered out)        │
│ Lead 4: branch-hn-001 ✓                        │
└─────────────────────────────────────────────────┘
```

## UI Display

```
┌─────────────────────────────────────────────────────────┐
│  📍 Lead & Placement Test                               │
│                                                          │
│  Nhận lead, phân công tư vấn... • Chi nhánh: Hà Nội    │◄── Branch name shown
│                                      ^^^^^^^^^^^^^^^^    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  📊 STATS (filtered by branch)                          │
│                                                          │
│  Tất cả: 24    Mới: 8    Đang tư vấn: 12               │◄── Only counts from HN branch
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  📋 LEADS TABLE (filtered by branch)                    │
│                                                          │
│  Lead 1 (HN) ✓                                          │
│  Lead 2 (HN) ✓                                          │◄── Only shows HN branch leads
│  Lead 3 (HN) ✓                                          │
└─────────────────────────────────────────────────────────┘
```

## Security Flow

```
┌─────────────────┐
│  Frontend       │
│  Filter by      │
│  branchId       │
└────────┬────────┘
         │
         │ GET /api/leads?branchId=branch-hn-001
         │
         ▼
┌─────────────────────────────────┐
│  Backend Middleware (REQUIRED)  │
│                                 │
│  1. Verify JWT token            │
│  2. Extract user's branchId     │
│  3. Compare with request param  │
│  4. REJECT if mismatch          │
└────────┬────────────────────────┘
         │
         │ ✓ Authorization passed
         │
         ▼
┌─────────────────┐
│  Database       │
│  Query with     │
│  WHERE branchId │
└─────────────────┘
```

## Multi-Branch Scenario (Different Staff)

```
Staff A (Hà Nội - branch-hn-001)      Staff B (HCM - branch-hcm-002)
         │                                      │
         ▼                                      ▼
┌──────────────────┐                  ┌──────────────────┐
│ branchId:        │                  │ branchId:        │
│ branch-hn-001    │                  │ branch-hcm-002   │
└────────┬─────────┘                  └────────┬─────────┘
         │                                      │
         ▼                                      ▼
  ┌─────────────┐                        ┌─────────────┐
  │ Sees Leads: │                        │ Sees Leads: │
  │  - Lead 1   │                        │  - Lead 5   │
  │  - Lead 2   │                        │  - Lead 6   │
  │  - Lead 3   │                        │  - Lead 7   │
  │  - Lead 4   │                        │  - Lead 8   │
  └─────────────┘                        └─────────────┘
       HN only                                HCM only
```
